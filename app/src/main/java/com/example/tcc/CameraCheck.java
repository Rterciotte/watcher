package com.example.tcc;

public class CameraCheck{





}
/*
@RequiresApi(api = Build.VERSION_CODES.LOLLIPOP)
public class CameraCheck<handler> {

    Context context = getApplicationContext();
    private CameraManager mCameraManager = (CameraManager) Context.getSystemService(CAMERA_SERVICE);;
    private Object mCameraCallback;
    private String cameraId;
    Handler handler = new Handler();



    public void onCameraAvailable(String cameraId) {
        if (mCameraManager)
                        Toast.makeText(context, "camera off", Toast.LENGTH_SHORT).show();
            }


    public void onCameraUnavailable(String cameraId) {

                Toast.makeText(context, "camera on", Toast.LENGTH_SHORT).show();
            }


        mCameraManager.registerAvailabilityCallback((CameraManager.AvailabilityCallback) mCameraCallback, handler);
    }

*/